# LeniaLernt

## Credentials

Create a `.env` file (copy from `.env.example`) and set your API key: `OPENAI_API_KEY` or `AZURE_OPENAI_API_KEY`. The project respects `.gitignore` which excludes `.env`.

## Run

Install requirements: `pip install -r requirements.txt` and then run `python shrink.py`. 